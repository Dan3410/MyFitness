import { FC, useEffect, useState } from 'react';
import styles from './stepsList.module.scss';

interface StepsListProps { }

const StepsList: FC<StepsListProps> = () => {

  useEffect(() => {
    
    }, [])

  return (<>
  </>
  )
};

export default StepsList;
