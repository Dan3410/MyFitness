package com.example.myFitness.workout.model;

public enum StepType {
    // General
    REST,
    SET,

    // Gym
    GYM_EXERCISE,
    GYM_INTERVAL,
    GYM_WARMUP,
    GYM_COOLDOWN,

    // Swim
    SWIM_DISTANCE,
    SWIM_TIME,
    SWIM_WARMUP,
    SWIM_COOLDOWN,

    // Run
    RUN_DISTANCE,
    RUN_TIME,
    RUN_CALORIES,
    RUN_WARMUP,
    RUN_COOLDOWN
}
